# Visual Guide: How the Framework Works

This document provides visual representations of how the Semantic Parser Framework operates.

## 🔄 Complete Reasoning Loop

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INITIATES PARSE                         │
│                  engine.parse(query, target_format)                  │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      INITIALIZATION PHASE                            │
│  • Create ReasoningTrace                                            │
│  • Set query and target format                                      │
│  • Initialize step counter                                          │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
                                 ▼
         ┌───────────────────────────────────────┐
         │      START REASONING LOOP             │
         │      (Max 10 steps by default)        │
         └───────────┬───────────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────────────┐
    │         STEP N: PROMPT BUILDING                │
    │  ┌──────────────────────────────────────────┐ │
    │  │ System Prompt:                           │ │
    │  │ • ReACT instructions                     │ │
    │  │ • Output format rules                    │ │
    │  └──────────────────────────────────────────┘ │
    │  ┌──────────────────────────────────────────┐ │
    │  │ User Prompt:                             │ │
    │  │ • Original query & target format         │ │
    │  │ • Available actions with schemas         │ │
    │  │ • Previous steps (history)               │ │
    │  │ • Step counter                           │ │
    │  └──────────────────────────────────────────┘ │
    └────────────────┬───────────────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────────────┐
    │           LLM REASONING PHASE                  │
    │  ┌──────────────────────────────────────────┐ │
    │  │ LLM receives complete prompt             │ │
    │  │ LLM reasons using chain-of-thought       │ │
    │  │ LLM outputs:                             │ │
    │  │   • Thought: reasoning explanation       │ │
    │  │   • Action: chosen action name           │ │
    │  │   • Action Input: parameters as JSON     │ │
    │  └──────────────────────────────────────────┘ │
    └────────────────┬───────────────────────────────┘
                     │
                     ▼
    ┌────────────────────────────────────────────────┐
    │         RESPONSE PARSING                       │
    │  • Extract "Thought" section                   │
    │  • Extract "Action" name                       │
    │  • Parse "Action Input" JSON                   │
    │  • Validate structure                          │
    └────────────────┬───────────────────────────────┘
                     │
                     ▼
              Is action "finish"?
                     │
        ┌────────────┴────────────┐
        │ YES                     │ NO
        ▼                         ▼
    ┌───────────┐      ┌──────────────────────────────┐
    │  COMPLETE │      │   ACTION EXECUTION PHASE     │
    │  Extract  │      │ ┌──────────────────────────┐ │
    │  final    │      │ │ Lookup action in         │ │
    │  output   │      │ │ ActionRegistry           │ │
    │  Return   │      │ └──────────────────────────┘ │
    │  trace    │      │ ┌──────────────────────────┐ │
    └───────────┘      │ │ Validate input params    │ │
                       │ │ against schema           │ │
                       │ └──────────────────────────┘ │
                       │ ┌──────────────────────────┐ │
                       │ │ Execute action.execute() │ │
                       │ └──────────────────────────┘ │
                       │ ┌──────────────────────────┐ │
                       │ │ Get ActionOutput:        │ │
                       │ │ • success: bool          │ │
                       │ │ • result: data           │ │
                       │ │ • error: message         │ │
                       │ └──────────────────────────┘ │
                       └──────────┬───────────────────┘
                                  │
                                  ▼
                       ┌─────────────────────────┐
                       │ OBSERVATION PHASE       │
                       │ • Create Observation    │
                       │ • Create ReasoningStep  │
                       │ • Add to trace history  │
                       └──────────┬──────────────┘
                                  │
                                  ▼
                       ┌─────────────────────────┐
                       │ Increment step counter  │
                       │ Check if max reached    │
                       └──────────┬──────────────┘
                                  │
                                  ▼
                       ┌─────────────────────────┐
                       │ Loop back to STEP N+1   │
                       │ (with updated context)  │
                       └─────────────────────────┘
```

## 🎯 Action Lifecycle

```
┌─────────────────────────────────────────────────┐
│              ACTION DEFINITION                   │
│                                                  │
│  class MyAction(Action):                        │
│      def __init__(self):                        │
│          super().__init__(                      │
│              name="my_action",                  │
│              description="..."                  │
│          )                                      │
│                                                 │
│      def get_input_schema(self):                │
│          return {...}                           │
│                                                 │
│      def execute(self, **kwargs):               │
│          # Implementation                       │
│          return ActionOutput(...)               │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│           ACTION REGISTRATION                    │
│                                                  │
│  registry = ActionRegistry()                    │
│  registry.register(MyAction())                  │
│                                                 │
│  Action stored in registry with:                │
│  • name: "my_action"                           │
│  • description: "..."                           │
│  • input_schema: {...}                         │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│           ACTION DISCOVERY                       │
│                                                  │
│  Engine builds prompt including:                │
│  • All action names                             │
│  • All action descriptions                      │
│  • All action schemas                           │
│                                                 │
│  LLM sees complete action catalog               │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│            ACTION SELECTION                      │
│                                                  │
│  LLM reasons about:                             │
│  • Current situation                            │
│  • Available actions                            │
│  • Which action fits best                       │
│                                                 │
│  LLM outputs: Action: my_action                 │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│            ACTION VALIDATION                     │
│                                                  │
│  1. Lookup action in registry                   │
│  2. Parse action input parameters               │
│  3. Validate against input schema               │
│  4. Check required fields present               │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│            ACTION EXECUTION                      │
│                                                  │
│  action.execute(**parameters)                   │
│                                                 │
│  Returns: ActionOutput(                         │
│      success=True/False,                        │
│      result=...,                                │
│      error=...,                                 │
│      metadata={...}                             │
│  )                                              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│          OBSERVATION RECORDING                   │
│                                                  │
│  Create Observation:                            │
│  • action_name                                  │
│  • result                                       │
│  • success                                      │
│  • error (if any)                               │
│                                                 │
│  Add to reasoning trace                         │
│  Available to LLM in next step                  │
└─────────────────────────────────────────────────┘
```

## 📊 Data Flow Diagram

```
┌─────────────┐
│    User     │
│   Request   │
└──────┬──────┘
       │
       │ query, target_format
       │
       ▼
┌──────────────────────┐
│   ReACT Engine       │
│                      │
│  ┌────────────────┐ │
│  │ Reasoning Loop │ │
│  └────────────────┘ │
└──┬────────┬────────┬┘
   │        │        │
   │        │        │
   ▼        ▼        ▼
┌─────┐ ┌──────┐ ┌─────────┐
│ LLM │ │Action│ │Reasoning│
│Client│ │Reg. │ │ Trace   │
└──┬──┘ └───┬──┘ └────┬────┘
   │        │         │
   │ prompt │         │
   ▼        │         │
┌─────────┐ │         │
│  Azure  │ │         │
│ OpenAI  │ │         │
│   o3    │ │         │
└────┬────┘ │         │
     │      │         │
     │ resp │         │
     ▼      │         │
   Parse    │         │
     │      │         │
     │ action_name    │
     │      │         │
     │      ▼         │
     │  ┌────────┐   │
     │  │Get     │   │
     │  │Action  │   │
     │  └───┬────┘   │
     │      │        │
     │      ▼        │
     │  ┌────────┐  │
     │  │Execute │  │
     │  │Action  │  │
     │  └───┬────┘  │
     │      │       │
     │      ▼       │
     │  ActionOutput│
     │      │       │
     │      ▼       │
     │  Observation │
     │      │       │
     │      ├───────┴──> Add to Trace
     │      │
     └──────┴──> Next Step (loop)
```

## 🏗️ Component Architecture

```
┌───────────────────────────────────────────────────────────┐
│                     APPLICATION LAYER                      │
│  (Your code using the framework)                          │
└─────────────────────┬─────────────────────────────────────┘
                      │
                      │ imports and uses
                      │
┌─────────────────────┴─────────────────────────────────────┐
│                   SEMANTIC PARSER FRAMEWORK                │
│                                                            │
│  ┌──────────────────────────────────────────────────┐    │
│  │               ReACT Engine                       │    │
│  │  • Orchestrates reasoning loop                   │    │
│  │  • Manages state and flow                        │    │
│  │  • Coordinates components                        │    │
│  └──┬────────────┬────────────┬─────────────────────┘    │
│     │            │            │                           │
│     ▼            ▼            ▼                           │
│  ┌─────┐    ┌──────┐    ┌──────────┐                    │
│  │ LLM │    │Prompt│    │ Reasoning│                    │
│  │Client│   │Builder│   │  Trace   │                    │
│  └──┬──┘    └───┬──┘    └────┬─────┘                    │
│     │           │            │                           │
│  ┌──┴───────────┴────────────┴──────┐                   │
│  │      Action Protocol Layer        │                   │
│  │  ┌─────────────────────────────┐ │                   │
│  │  │    Action Registry          │ │                   │
│  │  │  • Manages all actions      │ │                   │
│  │  │  • Provides discovery       │ │                   │
│  │  └───────────┬─────────────────┘ │                   │
│  │              │                    │                   │
│  │              ▼                    │                   │
│  │  ┌─────────────────────────────┐ │                   │
│  │  │    Action Base Class        │ │                   │
│  │  │  • Defines interface        │ │                   │
│  │  │  • Validation logic         │ │                   │
│  │  └──────────┬──────────────────┘ │                   │
│  └─────────────┼────────────────────┘                   │
│                │                                         │
└────────────────┼─────────────────────────────────────────┘
                 │
                 │ implements
                 │
┌────────────────┴─────────────────────────────────────────┐
│                  USER ACTION LAYER                        │
│  (Your custom actions)                                    │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Schema       │  │ Validation   │  │ Search       │  │
│  │ Action       │  │ Action       │  │ Action       │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Database     │  │ API Call     │  │ Your Custom  │  │
│  │ Action       │  │ Action       │  │ Actions...   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└───────────────────────────────────────────────────────────┘
```

## 🔀 Reasoning Step Structure

```
ReasoningStep
├── step_number: int
│   └─> Sequential identifier (1, 2, 3, ...)
│
├── thought: Thought
│   ├── content: str
│   │   └─> "I need to get the schema first..."
│   └── timestamp: datetime
│
├── action: ActionCall
│   ├── action_name: str
│   │   └─> "get_schema"
│   ├── parameters: Dict
│   │   └─> {"database": "users", "include_samples": true}
│   └── timestamp: datetime
│
└── observation: Observation
    ├── action_name: str
    │   └─> "get_schema"
    ├── result: Any
    │   └─> {schema data...}
    ├── success: bool
    │   └─> true
    ├── error: Optional[str]
    │   └─> None (or error message)
    ├── metadata: Dict
    │   └─> {"execution_time": 0.5, ...}
    └── timestamp: datetime
```

## 🎭 Example Reasoning Trace

```
Query: "Find all users older than 18"
Target: SQL

┌─────────────────────────────────────────────────────────┐
│ Step 1                                                   │
├─────────────────────────────────────────────────────────┤
│ Thought:                                                │
│  "I need to understand the database structure first.    │
│   Let me retrieve the schema to see what tables and     │
│   columns are available."                               │
│                                                         │
│ Action: get_schema                                      │
│ Input: {"database": "users"}                            │
│                                                         │
│ Observation:                                            │
│  SUCCESS: {                                             │
│    "tables": [                                          │
│      {                                                  │
│        "name": "users",                                 │
│        "columns": [                                     │
│          {"name": "id", "type": "INTEGER"},            │
│          {"name": "name", "type": "VARCHAR"},          │
│          {"name": "age", "type": "INTEGER"}            │
│        ]                                               │
│      }                                                 │
│    ]                                                   │
│  }                                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Step 2                                                   │
├─────────────────────────────────────────────────────────┤
│ Thought:                                                │
│  "Great! I can see there's a 'users' table with an     │
│   'age' column. Now I can write the SQL query to       │
│   select users where age is greater than 18."          │
│                                                         │
│ Action: finish                                          │
│ Input: {                                                │
│   "output": "SELECT * FROM users WHERE age > 18"       │
│ }                                                       │
└─────────────────────────────────────────────────────────┘

Final Output: SELECT * FROM users WHERE age > 18
Steps: 2
Duration: 4.2 seconds
Status: Complete ✅
```

## 🎨 Action Types Diagram

```
                    ┌──────────────┐
                    │    Action    │
                    │ (Base Class) │
                    └───────┬──────┘
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
          ▼                 ▼                 ▼
    ┌──────────┐     ┌──────────┐     ┌──────────┐
    │Information│    │Validation│     │Transform │
    │ Retrieval│     │          │     │          │
    └────┬─────┘     └────┬─────┘     └────┬─────┘
         │                │                 │
    ┌────┴────┐      ┌────┴────┐      ┌────┴────┐
    │         │      │         │      │         │
    ▼         ▼      ▼         ▼      ▼         ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│Schema│ │Search│ │Syntax│ │Schema│ │Rewrite│ │Format│
│Get   │ │Query │ │Check │ │Check │ │Query │ │Output│
└──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘

    Examples:           Examples:         Examples:
    • get_schema       • validate_sql    • rewrite_query
    • search_examples  • check_syntax    • optimize_query
    • api_call         • verify_schema   • format_output
```

## 📈 Execution Timeline

```
Time →

0s     ┌─────────────┐
       │Initialize   │
       │Engine       │
       └──────┬──────┘
              │
1s     ┌──────┴──────┐
       │Create Trace │
       └──────┬──────┘
              │
       ╔══════╧════════╗
       ║  STEP 1       ║
       ╚══════╤════════╝
2s            │
       ┌──────┴──────┐
       │Build Prompt │
       └──────┬──────┘
              │
3s     ┌──────┴──────┐
       │LLM Call     │
       │(2-3s)       │
       └──────┬──────┘
              │
6s     ┌──────┴──────┐
       │Execute      │
       │Action       │
       └──────┬──────┘
              │
7s     ┌──────┴──────┐
       │Record Step  │
       └──────┬──────┘
              │
       ╔══════╧════════╗
       ║  STEP 2       ║
       ╚══════╤════════╝
              │
       ... (repeat) ...
              │
20s    ┌──────┴──────┐
       │Finish       │
       │Return Trace │
       └─────────────┘
```

## 🎯 Summary Visualization

```
┌────────────────────────────────────────────────────────┐
│               SEMANTIC PARSER FRAMEWORK                 │
│                                                         │
│  INPUT: "Find all users older than 18"                 │
│         Target: SQL                                     │
│                                                         │
│  ┌──────────────────────────────────────────────────┐ │
│  │              ReACT REASONING LOOP                │ │
│  │                                                  │ │
│  │  Think → Act → Observe → Think → Act → ...     │ │
│  │                                                  │ │
│  │  Step 1: Get schema                             │ │
│  │  Step 2: Generate SQL                           │ │
│  │  Step 3: Validate (if needed)                   │ │
│  │  Step N: Finish                                 │ │
│  └──────────────────────────────────────────────────┘ │
│                                                         │
│  OUTPUT: "SELECT * FROM users WHERE age > 18"          │
│                                                         │
│  TRACE: Complete reasoning history with all steps      │
│                                                         │
└────────────────────────────────────────────────────────┘

   Powered by:
   • Azure OpenAI o3 (reasoning)
   • Custom Actions (capabilities)
   • Type-safe Models (reliability)
```

---

**Visual Guide Complete!**

Use these diagrams to understand how the framework operates at different levels of abstraction.
